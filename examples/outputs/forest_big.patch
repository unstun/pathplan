--- a/examples/forest_scene.py
+++ b/examples/forest_scene.py
@@ -57,7 +57,7 @@
     rng = np.random.default_rng(seed)
     centers = []
     min_spacing = tree_radius * 2.0 + clearance
-    max_attempts = num_trees * 50
+    max_attempts = num_trees * 300  # larger scenes / larger clearance need more sampling attempts
     margin = max(tree_radius + 0.3, tree_radius + clearance * 0.25)

     def dist_point_to_segment(p, a, b):
@@ -163,7 +163,7 @@
 ):
     if plt is None or not planner_results:
         return None
-    fig, ax = plt.subplots(figsize=(8, 5))
+    fig, ax = plt.subplots(figsize=(24, 14))
     h, w = grid_map.data.shape
     extent = [
         grid_map.origin[0],
@@ -198,7 +198,7 @@
     ax.legend(loc="upper right")
     out_dir.mkdir(parents=True, exist_ok=True)
     out_path = out_dir / "dense_forest_no_slope_many_trees.png"
-    fig.savefig(out_path, dpi=150, bbox_inches="tight")
+    fig.savefig(out_path, dpi=300, bbox_inches="tight")
     plt.close(fig)
     return out_path

@@ -206,22 +206,22 @@
 def plan_forest_scene():
     output_dir = Path(__file__).resolve().parent / "outputs"
-    start = AckermannState(2.0, 2.0, 0.0)
-    goal = AckermannState(18.0, 10.0, 0.0)
+    start = AckermannState(5.0, 5.0, 0.0)
+    goal = AckermannState(75.0, 43.0, 0.0)
     grid_map = make_forest_map(
-        resolution=0.05,
-        size=(20.0, 12.0),
-        num_trees=25,
-        tree_radius=0.35,
-        clearance=1.8,
+        resolution=0.10,
+        size=(80.0, 48.0),
+        num_trees=450,
+        tree_radius=0.30,
+        clearance=3.0,
         seed=13,
         keep_clear=[(start.x, start.y), (goal.x, goal.y)],
         carve_points=[
             (start.x, start.y),
-            (4.0, 4.0),
-            (7.5, 6.0),
-            (11.0, 7.5),
-            (14.5, 8.8),
+            (20.0, 18.0),
+            (40.0, 26.0),
+            (60.0, 35.0),
             (goal.x, goal.y),
         ],
-        carve_width=6.0,
+        carve_width=10.0,
         wall_y=None,
         wall_gap=(5.5, 1.2),
         wall_radius=0.10,
         protected_path=[
             (start.x, start.y),
-            (4.0, 4.0),
-            (7.5, 6.0),
-            (11.0, 7.5),
-            (14.5, 8.8),
+            (20.0, 18.0),
+            (40.0, 26.0),
+            (60.0, 35.0),
             (goal.x, goal.y),
         ],
-        protected_width=6.0,
+        protected_width=10.0,
     )

     params = AckermannParams()
     footprint = OrientedBoxFootprint(length=0.924, width=0.740)
-    short_primitives = default_primitives(params, step_length=0.5)
+    short_primitives = default_primitives(params, step_length=1.0)
     planner_kwargs = dict(
-        xy_resolution=0.25,
-        collision_step=0.15,
-        goal_xy_tol=1.0,
+        xy_resolution=0.60,
+        collision_step=0.20,
+        goal_xy_tol=1.5,
         goal_theta_tol=math.pi,
     )
@@ -267,9 +267,9 @@
     for label, planner in planners:
         t0 = time.time()
         if isinstance(planner, RRTStarPlanner):
-            path, stats = planner.plan(start, goal, max_iter=12000, timeout=20.0)
+            path, stats = planner.plan(start, goal, max_iter=60000, timeout=60.0)
         else:
-            path, stats = planner.plan(start, goal, timeout=30.0, max_nodes=40000)
+            path, stats = planner.plan(start, goal, timeout=60.0, max_nodes=120000)
         stats["time_wall"] = time.time() - t0
         success = len(path) > 0
         expansions = stats.get("expansions", stats.get("expansions_total", stats.get("nodes", "-")))
